# Аудит проекта (Codex 5.3) — 2026-03-08

## 1) Что проверено

- Подготовка окружения: `npm ci`, затем `npm run lint`, `npm run test`, `npm run build`.
- Полный проход по структуре git-репозитория (`git ls-files -z`) и модулей `src/`.
- Анализ автономности (работа без внешних сервисов).
- Анализ безопасности (frontend, env, CI/CD, заголовки, зависимости).
- Поиск мусора/легаси и сразу применимые исправления.

## 2) Снимок репозитория сейчас

- Всего git-tracked файлов: **256**
- Расклад по верхнему уровню:
  - `.cursor`: 1
  - `(root)`: 21
  - `.github`: 2
  - `.vscode`: 2
  - `docs`: 11
  - `max-bot`: 9
  - `public`: 191
  - `scripts`: 1
  - `src`: 18
- Расклад по типам (топ):
  - `.jpg`: 163
  - `.svg`: 25
  - `.md`: 16
  - `.js`: 11
  - `.jsx`: 11

Вывод: проект в первую очередь контентный (много медиа в `public/`), а кодовая часть компактная.

## 3) Проблемы, исправленные сразу

1. **Безопасность/автономность лид-форм**
   - В `src/api/leads.js` убран «тихий успех» в production при пустом `VITE_LEAD_API`.
   - Добавлены таймаут запроса и корректная обработка не-JSON ответов webhook.
   - В payload отправляется безопасный `page` без query/hash.

2. **Безопасность ссылок из env**
   - Добавлен `src/utils/safeExternalUrl.js`.
   - В `Hero.jsx` и `App.jsx` ссылка на Telegram теперь проходит валидацию (`https` + `t.me/telegram.me`).

3. **Gemini API вызов**
   - В `src/api/gemini.js` ключ убран из query-string в HTTP header `x-goog-api-key`.
   - Добавлена поддержка `VITE_GEMINI_MODEL` и таймаут запроса.

4. **Харденинг деплоя**
   - В `vercel.json` добавлены security headers: CSP, `X-Frame-Options`, `Referrer-Policy`, `X-Content-Type-Options`, `Permissions-Policy`.
   - В `.github/workflows/deploy.yml` добавлены quality gates: `lint` + `test` + `build` до deploy.
   - Из deploy-триггера удалена нестандартная ветка, оставлены `main/master`.

5. **Конфиги и документация**
   - В `.env.example` удалены команды shell (оставлены только env-переменные).
   - В `docker-compose.yml` исправлен `WEBHOOK_URL` и ограничен bind порта n8n на localhost.
   - В `AGENTS.md` исправлена информация про наличие lint/test и поведение env.
   - В `README.md` синхронизированы шрифты и команда установки (`npm ci`).

6. **Очистка мусора/легаси**
   - Удалены неиспользуемые компоненты-дубликаты:
     - `src/components/layout/Navbar.jsx`
     - `src/components/layout/BackToTop.jsx`
     - `src/components/layout/Reveal.jsx`
   - Удалён одноразовый legacy-скрипт `scripts/extract-pdf-images.js` (жёсткий локальный Windows-путь).
   - Удалён stale gitlink `vozduh-updated-fixed3photo`.
   - Удалены неиспользуемые PDF-зависимости и связанные уязвимости в lockfile.

## 4) Анализ автономности

### Текущее состояние
- Базовый SPA-функционал автономен: каталог, секции, модалки, калькулятор работают локально без backend.
- Интеграции:
  - Лиды: `VITE_LEAD_API`
  - AI: `VITE_GEMINI_API_KEY` (+ теперь `VITE_GEMINI_MODEL`)

### Что улучшено
- Раньше production мог показывать «успех» отправки лида без API.
- Теперь production явно сигнализирует об ошибке конфигурации, чтобы лиды не терялись скрыто.

## 5) Анализ безопасности

### Основные риски (и что сделано)
- Риск небезопасной env-ссылки в `href` → добавлена валидация URL.
- Риск утечки API-ключа через query-string → ключ перенесён в header.
- Отсутствие security headers на деплое → добавлены в `vercel.json`.
- Уязвимые транзитивные зависимости PDF-цепочки → удалены неиспользуемые пакеты.

### Статус уязвимостей npm
- До правок: 12 (включая high).
- После очистки и обновления tooling: 0.

## 6) Детальное workflow-дерево (как сейчас)

```text
Runtime (пользовательский путь)
main.jsx
└─ App.jsx (MainSite)
   ├─ Navbar (sections/Navbar.jsx)
   ├─ Hero (sections/Hero.jsx) -> openCalc(tab)
   ├─ Catalog -> SolutionDetailModal -> LeadModal -> postLead()
   ├─ TurkovPromo -> TurkovCatalogModal -> TurkovCategoryModal -> LeadModal -> postLead()
   ├─ BrandMarquee
   ├─ WorksMarquee
   ├─ WhyChooseUsSection
   │  ├─ ServiceInfoModal -> LeadModal
   │  └─ BriefGeneratorModal -> fetchGeminiResponse()
   ├─ PartnersSection -> PartnerModal -> postLead()
   ├─ ContactForm -> LeadModal / ContactModal -> postLead()
   ├─ CTAstrip
   ├─ Footer
   ├─ ClimateAssistant -> fetchGeminiResponse()
   └─ QuickCalcModal (query-driven: ?calc=1&tab=...)

Infrastructure
package.json scripts
├─ dev: vite
├─ lint: eslint src
├─ test: vitest run
├─ build: vite build
└─ preview: vite preview

Deploy
.github/workflows/deploy.yml
└─ npm ci -> lint -> test -> build -> vercel deploy

Hosting
vercel.json
├─ security headers
└─ SPA rewrite /(.*) -> /index.html
```

## 7) Рекомендуемое workflow-дерево (улучшенная версия)

```text
src/
├─ app/
│  ├─ AppShell.jsx                # только композиция страницы
│  └─ providers/
├─ features/
│  ├─ leads/
│  │  ├─ api/postLead.js
│  │  ├─ ui/LeadModal.jsx
│  │  └─ model/useLeadForm.js
│  ├─ assistant/
│  │  ├─ api/getAssistantReply.js # через backend proxy
│  │  └─ ui/ClimateAssistant.jsx
│  ├─ calc/
│  │  ├─ ui/QuickCalcModal.jsx
│  │  └─ model/calcRules.js
│  └─ catalog/
│     ├─ ui/Catalog.jsx
│     └─ ui/SolutionDetailModal.jsx
├─ widgets/
│  ├─ Navbar/
│  ├─ Hero/
│  ├─ ContactForm/
│  └─ Footer/
├─ shared/
│  ├─ ui/Reveal.jsx
│  ├─ ui/BackToTop.jsx
│  └─ lib/safeExternalUrl.js
└─ data/
   ├─ solutions.js
   ├─ services.js
   ├─ presets.js
   └─ turkov.js

External integrations (target)
Frontend -> /api/leads (server-side validation/rate-limit) -> n8n/CRM
Frontend -> /api/assistant (server-side Gemini key) -> Google API
```

## 8) Что ещё осталось (следующий этап)

- Разделить монолитный `src/App.jsx` на feature-модули.
- Убрать оставшиеся legacy-документы или перенести их в `docs/archive/`.
- Добавить e2e smoke-тесты ключевых пользовательских сценариев (лид, калькулятор, AI-чат).
